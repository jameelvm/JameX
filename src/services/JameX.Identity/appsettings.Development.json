{
  // Host-run defaults; docker-compose overrides these with container addresses.
  // See JameX.Catalog/appsettings.Development.json for the full reasoning.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "ConnectionStrings": {
    "Identity": "Host=localhost;Database=jamex_users;Username=jamex;Password=jamex",
    "Redis": "localhost:6379"
  },

  "Aws": {
    "ServiceUrl": "http://localhost:4566",
    "PublicServiceUrl": "http://localhost:4566",
    "Region": "us-east-1",
    "AccessKey": "test",
    "SecretKey": "test"
  },

  "Messaging": {
    "TopicName": "jamex-video-events"
  },

  "Jwt": {
    "Issuer": "JameX.Identity",
    "Audience": "JameX",
    "SigningKey": "dev-only-signing-key-never-use-in-production-3f8a9c2e7b1d4f6a",
    "ExpiryMinutes": 60
  }
}
